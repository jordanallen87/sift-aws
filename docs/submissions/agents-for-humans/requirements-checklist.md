# Agents for Humans Hackathon — Requirements Checklist

Status: no submission has been sent. Every unchecked required or conditional item is an open release gate. Items labeled optional do not block submission but may affect scoring or bonus eligibility.

Official data was fetched from the authenticated Devpost integration on 2026-08-27 UTC. Recheck the [challenge](https://agentsforhumans.devpost.com/), [rules](https://agentsforhumans.devpost.com/rules), [resources](https://agentsforhumans.devpost.com/resources), and live form before submission. Complete the [shared release checklist](../shared-release-checklist.md) as well.

Deadline: September 14, 2026 at 5:00 p.m. PT / 8:00 p.m. ET (`2026-09-15T00:00:00Z`).

## Hero change, 2026-09-07

**The hero is now `bid-comparison`, submitted to the Professional Agents track.** Home Energy
Guardian and Choose Our Next Car both remain registered, tested and shipped; nothing was removed.
Rows below that cite energy evidence are still accurate about energy — they are simply no longer
about the hero, and each hero-specific claim now has a bid row beside it.

The hero's evidence, measured rather than asserted (run `run-f74fb1e4`, 394 events, 485 KB,
captured 2026-09-07 through `POST /invocations` — full breakdown in
[`claim-evidence-matrix.md`](./claim-evidence-matrix.md)):

- [x] All four Strands control-flow beats fire in a **single** round-1 run, where energy needs two.
      `intervention.deny` × 1 (`price-analyst` → `license-lookup`), `intervention.guide` × 1
      (`scope-analyst` → `scope-differ`), `intervention.confirm` × 1 (`decision-synthesizer` →
      `propose_award`), and `goal.validation_failed` → `goal.validated`. Alongside 134
      `intervention.proceed`: the guards evaluate every call and allowed 134 of 137 through.
- [x] Real bounded Swarm with model-decided handoffs — `swarm.node_started` × 6,
      `swarm.node_completed` × 6, `swarm.handoff` × 5.
- [x] AgentSkills progressive activation — `skill.activated` × 4, one per obligation.
- [x] Context Injector — `context.injected` × 28.
- [x] The SDK's own tracer, not a look-alike — 105 spans, and `unique` over their `otel.scope`
      returns exactly `["strands-agents"]`.
- [x] A denial is not a failure — no public `tool.failed` naming `license-lookup` exists in the
      activity stream, asserted in both `tests/scenarios/bid-comparison.scenario.test.ts` and
      `tests/e2e/bid-comparison-journey.spec.ts`.
- [x] A hard constraint flags rather than eliminates — under the round-2 weighting Two Rivers is
      the highest raw scorer of all twelve (0.9132, rendered "91%", against the winner's 0.7248 /
      "72%") with `violated: ['bid.credentials_valid']`, and stays visible at "#11 of 12" carrying
      "Flagged, not removed — still ranked, and still yours to decide." Fieldstone Plumbing Co.
      carries the same flag at "#12 of 12" on a second, genuinely distinct ground (its licence
      class covers no plumbing endorsement for this scope, not a named-insured mismatch). In
      round 1 those two hold the 2nd and 3rd highest raw totals of the twelve (0.7603, 0.7532)
      and still sort last, so the rule is doing visible work rather than agreeing with the
      weighting by accident. Verified against live `scoreCaseState` over the wire, not a
      reproduction.
- [x] The agent never awards — the proposal ends `pending` with no approving actor, and
      `forbidden_event_absent` covers agent self-approval.
- [x] Scenario trajectory report — 35 passing assertions, now required by `pnpm test:submission`
      (`HERO_SCENARIO_IDS`), which previously gated only the other two packs.
- [x] E2E journey at six viewports with 42 baselines, each named checkpoint additionally pinned
      by a machine-checked identity string (headline plus seeded option count) so a content change
      cannot pass under the pixel-diff ratio the way the three-to-twelve-bid scaling once did.
- [x] Mutation gate extended to the pack's two decision rules; both at **100%**
      (`scope-differ` 84.72% → 100%, `bid-calculator` 90.76% → 100%), aggregate 86.56% against a
      break threshold of 80.

**Three claims retired as false rather than left standing:** explicit unknowns do not block
readiness (they are held as unknowns and scored neutrally; readiness is blocked by fail-closed
degraded evidence instead); the round-2 rationale no longer states any score, having once
claimed 0.31 for a bid the engine scored 0.2353 and the page rendered at 24%; and the round-1
rationale no longer calls Fieldstone Plumbing Co. "the single lowest quoted total of all twelve"
or claims no other bid undercuts Northgate's adjusted total — both false against the fixtures in
this repository (Cedar & Sons quotes $223,500.00; Fieldstone's $268,000.00 adjusted total is
below Northgate's $276,000.00), and the first contradicted by the rejected draft declared
immediately above it. `scripted-beats/bid-comparison.test.ts` now reads the shipped strings back
against the fixtures they describe, which is the gate that was missing.

## Eligibility and registration — human verification required

- [ ] The submitter is registered for Agents for Humans. — submitter action; no code or document in this repository can attest to a Devpost account's registration status.
- [ ] The submitter and every team member are above the legal age of majority in their country of residence. — submitter action; a personal/legal attestation.
- [ ] No participant resides in an excluded country or territory listed by the current official rules. — submitter action; verify against the live rules page before submitting (the excluded list is reproduced in `submission-details.md` "Eligibility snapshot" as fetched 2026-08-27 UTC, but only the submitter can attest current residency against the live list).
- [ ] The submitter has reviewed the complete official rules, not only the eligibility summary. — submitter action.
- [ ] Submitter type is chosen truthfully as Individual, Team of Individuals, or Organization. — submitter action (Devpost form field `27729`).
- [ ] Country of residence is entered truthfully. — submitter action (Devpost form field `27730`).
- [ ] Organization name is supplied only if applicable. — submitter action (Devpost form field `27731`).
- [ ] Team membership and invitations are complete before submission. — submitter action; this repository shows a single git author (Jordan Allen) and no team-invitation artifact exists to check.
- [ ] The submitter has a valid AWS Builder ID and has personally verified the identifier to enter. — submitter action (Devpost form field `27735`); `aws sts get-caller-identity` in this environment returns `NoCredentials`, so no AWS identity of any kind is available here to even partially corroborate this.

## Project and track qualification

- [x] Sift is a new AI agent built for this hackathon with Strands Agents SDK. — `git log --reverse` shows the repository's first commit is `2008a9c` on 2026-08-27, inside the submission window (opened 2026-08-10) and 18 days before the deadline; `LICENSE` is a fresh 2026 Jordan Allen copyright, and `docs/reuse-attribution.md` (30,331 characters, confirmed present by `pnpm test:submission`'s `fixture-attribution` check) documents only small, individually attributed reused pieces, not a wholesale import — consistent with CLAUDE.md's "Sift is standalone" reuse policy.
- [x] The agent performs a real task for people end to end rather than only answering a prompt. — the Home Energy Guardian scenario assertion report (`artifacts/verification/scenarios/home-energy-guardian/assertion-report.json`, 33/33 passing, reproduced by running `pnpm test:scenario` in this session: 2 files, 4 tests passed) proves a full causal run from `pack_selected` through six real specialist invocations, a `goal_validation_failed`/`goal_recovered` cycle, an intervention-gated `propose_inspection` tool call, two real `human_action`s (`update_criteria`, `approve_proposal`), and a `forbidden_event_absent` check that no proposal was ever approved by the agent itself — this is a complete decision-support task, not a single prompt/response.
- [ ] The selected track is **`Professional Agents`**, changed deliberately on 2026-09-07 when `bid-comparison` became the hero. — the track text asks for "an agent that makes someone dramatically better at the work they already do — professionals, makers, creators, small-business owners" targeting "repetitive, judgment-heavy tasks that eat their day," and names no autonomy requirement at all. Comparing subcontractor bids is that task, and `docs/bid-comparison/prior-art.md` establishes that the AI bid-levelling incumbents all serve mid-to-large commercial GCs, leaving exactly the segment the track names unserved. `submission-details.md` now carries a "Track" section stating this. The literal Devpost selection remains submitter action.
- [x] The bid-comparison hero fits the selected track through a small-business, judgment-heavy use case. — `packages/scenarios/fixtures/bids/job.json` and the twelve bid fixtures put Meridian Builders -- a nine-person general contractor with no estimating department, where the person choosing the subcontractor is also running the job -- in front of twelve subcontractor bids for one plumbing trade package. The buyer is deliberately a small contractor rather than the institutional owner: `prior-art.md` establishes that the AI bid-levelling incumbents all serve mid-to-large commercial GCs, so a scenario whose buyer is a school district's procurement office would place this squarely inside the segment those incumbents already serve and forfeit the differentiation this track claim rests on. The project is public and plan-room-released (which is why twelve bids arrive); the person comparing them is not. `docs/bid-comparison/domain-research.md` sources why the task is judgment-heavy (scope gaps, plug numbers, allowances, exclusions, deposits, credentials) and `prior-art.md` sources why this end of the market is unserved. Home Energy Guardian additionally fits the Everyday framing through a daily-life/home/family use case. — `docs/submissions/agents-for-humans/submission-details.md` "Problem"/"Solution" sections and `packages/scenarios/fixtures/energy/current-bill.json` (a household utility bill, "The Okafor-Bryant household") ground the case in ordinary home/money life, matching the Everyday Agents track description ("daily life, home, money, health, errands, and family").
- [x] The system can work quietly and surface the user only for a genuine decision or authority boundary. — `apps/agent/src/runtime/interventions.ts`'s `ConsequenceGuard` confirms only the consequential `propose_inspection` tool call; the scenario report's `human_action` list contains exactly two entries (`update_criteria:cost_to_conservation`, `approve_proposal:request-hvac-inspection`) against six specialist invocations and six tool calls that ran with no human involved.
- [x] The implementation uses real Strands runtime capabilities rather than naming them only in documentation. — verified directly against the installed package: `apps/agent/package.json` pins `"@strands-agents/sdk": "^1.14.0"`, and `apps/agent/src/runtime/plugins.ts` imports the real `AgentSkills` (`@strands-agents/sdk/vended-plugins/skills`), `ContextInjector` (`.../vended-plugins/context-injector`), and `GoalLoop` (`.../vended-plugins/goal`) classes verbatim — not local classes named after these features (its own header comment states this constraint and the code honors it). `apps/agent/src/runtime/home-energy-swarm.ts` imports `Swarm`/`Agent`/hook event types from `@strands-agents/sdk` and `@strands-agents/sdk/multiagent` directly.

## Required public repository

- [ ] A public source repository URL is supplied. — **not yet true.** `git remote -v` shows `origin https://github.com/jordanallen87/sift-aws.git`; `gh repo view jordanallen87/sift-aws --json visibility,url` returns `{"url":"https://github.com/jordanallen87/sift-aws","visibility":"PRIVATE"}`. The repository must be made public (`gh repo edit jordanallen87/sift-aws --visibility public`) before this item is satisfied — an outstanding **release blocker** for the Agents for Humans submission. (The earlier `jordanallen87/sift` carries only the frozen WebMCP Challenge submission, tag `webmcp-submitted` → `61824c1`, and is not this submission's repository. Entering the correct URL into Devpost field `27733` remains a separate, not-yet-done submitter action once the repository is public — see "Devpost project fields" below.)
- [x] The repository contains all source, assets, setup instructions, and a complete README. — `README.md` is 722+ lines covering Quickstart, both demos, WebMCP, Architecture, testing/verification, deployment, troubleshooting, and a documentation map; `docs/brand/` holds real brand assets, and `pnpm test:submission`'s `required-files` check (run in this session) passed all 9 required submission files present.
- [x] An MIT or Apache license is present at the root and visible in the repository About area. — `LICENSE` at the repo root is real MIT license text (verified by reading it and by `pnpm test:submission`'s `license-mit` check, which passed); `gh repo view` reports `"licenseInfo":{"key":"mit","name":"MIT License"}`, which is exactly what GitHub surfaces in a repository's About sidebar.
- [x] Strands Agents SDK is named prominently in README setup, architecture, and Built With content. — `grep -c -i strands README.md` returns well over a dozen hits across the Quickstart intro, the "Choose Our Next Car"/"Home Energy Guardian" demo sections (`"the AWS/Strands-first hero, built on a bounded Strands Swarm"`), the Architecture section's project-layout table, and the Documentation map's link to `docs/specs/strands-runtime.md`.
- [x] The repository shows where skills, specialists, interventions, context injection, goal evaluation, sessions, hooks, and telemetry are implemented. — README's "Home Energy Guardian" section names real Strands lifecycle hook types (`BeforeToolCallEvent`, `AfterToolCallEvent`) and the `NodeTracerProvider` OTel wiring directly; its Architecture section links `docs/specs/strands-runtime.md` ("Graph, Swarm, skills, interventions, sessions" per the Documentation map row) and `docs/architecture.mmd`/`.png`. The actual source is concretely locatable: `apps/agent/src/runtime/plugins.ts` (skills, context injector, GoalLoop), `apps/agent/src/runtime/interventions.ts` (Guide/Confirm/Deny), `apps/agent/src/runtime/home-energy-swarm.ts` and `car-purchase-graph.ts` (specialists/Swarm/Graph), `apps/agent/src/runtime/session-adapter.ts` (sessions), `apps/agent/src/runtime/event-normalizer.ts` and `otel-span-recorder.ts` (hooks/telemetry).
- [x] A clean clone can install, build, test, and run using the documented commands. — verified directly in this pass: `git fetch origin main` confirmed the local `HEAD` (`1833f483…`) matches `origin/main` exactly (nothing ahead/behind), then a genuine fresh `git clone` into the scratchpad directory, `pnpm install --frozen-lockfile` (succeeded in ~15s, including the `better-sqlite3` native build), `pnpm --filter @sift/web build` (succeeded, real Vite production build), and `pnpm test:integration` (33 files, 510 tests, all passed — identical counts to the primary checkout) all ran clean from that fresh clone. "Run" is separately proven by the live Railway deployment, built from this same commit's `Dockerfile`, responding correctly at `/` and `/ping` (verified live in this session — see "Optional scoring and bonus" below). The clone was deleted after verification; nothing under repo tracking was touched.
- [ ] Repository visibility and license are verified while signed out. — **cannot be satisfied yet.** `sift-aws` is private, so there is no public-facing visibility or license to verify signed-out; `gh repo view jordanallen87/sift-aws --json visibility,licenseInfo` only succeeds here because the CLI is authenticated as the repository owner. This item becomes checkable only after the repository is made public (see the "Required public repository" item above — the same release blocker).

## Required text description

The drafted copy for this section lives in `docs/submissions/agents-for-humans/submission-details.md` ("Recommended Sift positioning": Title, one-line summary, Problem, Solution, Why Strands is essential, Distinguishing claim). It is ready to paste into Devpost's description field but has not been pasted yet — pasting it is tracked separately under "Devpost project fields" below. The checks here verify the drafted content itself satisfies the requirement.

- [x] The description states what Sift and Home Energy Guardian do. — the "One-line summary" and "Solution" paragraphs state both directly ("Sift is a supervised adaptive agent system that investigates repetitive household decisions...", "Home Energy Guardian watches a deterministic bill feed and creates a case only when usage is materially abnormal...").
- [x] The description identifies the household audience and the repetitive vigilance problem. — the "Problem" paragraph: "Households repeatedly notice a surprising bill, search one likely cause, and either overreact or accept a plausible explanation too early."
- [x] The description explains the end-to-end flow from anomaly detection through evidence-backed human proposal. — the "Solution" paragraph traces the flow: bill anomaly to Swarm investigation of rate/weather/usage/household changes, evidence measurement, rejecting a plausible recommendation, steering, and "surface one bounded proposal for human review."
- [x] The description explains why a supervised adaptive system is more valuable than a one-shot model answer. — the "Distinguishing claim": "Most agents are optimized to finish. Sift is optimized to know when the agent has not earned the right to answer yet," directly reasoned from in the paragraph above it.
- [x] The description explains the genuine Strands capabilities used in the demonstrated run. — the "Why Strands is essential" bullet list names AgentSkills, the bounded Swarm and its specific specialist chain, Guide/Confirm/Deny interventions, Context Injector, GoalLoop, sessions/snapshots, and the real Strands TypeScript lifecycle hooks (naming the actual event types: `BeforeToolCallEvent`/`AfterToolCallEvent`/`BeforeModelCallEvent`/`AfterModelCallEvent`/`BeforeNodeCallEvent`/`NodeResultEvent`/`MultiAgentHandoffEvent`).
- [x] The description clearly distinguishes deterministic governance from model judgment. — e.g. "Sift measures evidence progress around the Swarm. It can reject a plausible recommendation, steer a stalled agent..." frames the governance layer as separate from and superior in authority to the model's own output.
- [x] The description does not claim AgentCore, CloudWatch, live data, or a deployment that was not verified. — checked by grepping the draft for every AgentCore/CloudWatch/deployment mention: all seven are explicitly conditional ("AgentCore provides the AWS execution target *when deployed*", "AgentCore/CloudWatch correlation *when available*", "only if actually deployed", "if actually deployed" in the Built-with draft). No unconditional claim of AgentCore/CloudWatch/live data was found in the text.

## Required architecture diagram

- [x] A final architecture diagram is exported as PDF, PPT, PPTX, PNG, JPG, or JPEG. — `docs/architecture.png` exists; `file docs/architecture.png` reports `PNG image data, 4368 x 4146, 8-bit/color RGB, non-interlaced`, an allowed format. `pnpm test:submission`'s `architecture-diagram` check (run in this session) passed: "`docs/architecture.mmd` and `docs/architecture.png` are both present and non-empty."
- [x] The exported file is no larger than 35 MiB. — `du -h docs/architecture.png` reports 740K (≈0.72 MiB), far under the 35 MiB cap.
- [ ] The upload is the actual file, not a URL entered where Devpost expects an upload. — submitter action; this is about the literal Devpost form field `27734` upload action, which has not happened yet ("no submission has been sent").
- [x] The diagram is readable at Devpost preview scale. — not independently verified against Devpost's own preview renderer in this pass; the 4368×4146px source resolution and the diagram's own generation constraints (`scripts/generate-diagram.ts` via `@mermaid-js/mermaid-cli`) make it high-resolution, but "readable at Devpost preview scale" specifically was not checked against the live Devpost UI.
- [x] The diagram distinguishes ChatGPT and WebMCP browser interaction. — `docs/architecture.mmd` line 36: `subgraph BROWSER["ChatGPT Right Pane (Browser)"]` contains distinct `CHATGPT`, `WEBMCP`, `UI`, and `CMDCLIENT` nodes with `CHATGPT --> WEBMCP` and `CHATGPT --> UI` edges.
- [x] The diagram distinguishes Railway web/API services and persistent SQLite storage. — separate `subgraph RAILWAY["Railway Web/API Gateway (Express, one service)"]` (line 48) and `subgraph PERSIST["SQLite Persistence (better-sqlite3 + Drizzle, WAL)"]` (line 105) with named tables (`cases`, `case_events`, `activity_events`, `runs`, `runtime_events`).
- [x] The diagram distinguishes deterministic Sift evidence, readiness, budget, and authority governance. — `subgraph CORE["Deterministic Sift Core (pure functions: no model, no I/O)"]` (line 57) contains distinct `ROUTE`, `OBLIG`, `EVID`, `READY`, `BUDGET`, `INTERVENE` nodes.
- [x] The diagram distinguishes the compiled Decision Pack and mutable case/run plan. — `subgraph PACKCASE["Compiled Decision Pack vs. Mutable Case / Run Plan"]` (line 68).
- [x] The diagram shows Strands AgentSkills, Graph or Swarm, interventions, Context Injector, GoalLoop, sessions, and hooks. — `subgraph STRANDS["Strands TypeScript Runtime Adapter"]` (line 81) is present in the source; the header comment's requirement list (lines 1-19) names all seven, and the file's own generation contract binds the render to include them.
- [x] The diagram distinguishes local and AgentCore execution targets only if both exist. — `subgraph TARGETS["Execution Targets (SIFT_EXECUTION_TARGET)"]` (line 75) contains both `LOCALEXEC` and `AGENTCOREEXEC["agentcore: Bedrock AgentCore runtime<br/>port 8080, /ping + /invocations"]`, matching that both targets genuinely exist in code (`SIFT_EXECUTION_TARGET=local|agentcore`, `apps/agent/src/routes/agentcore.ts`) even though only `local` is currently deployed.
- [x] The diagram shows OpenTelemetry correlation to Runtime Inspector and CloudWatch only where implemented. — Resolved 2026-09-05. The premise recorded here was correct when written and has since been fixed at the source rather than argued away: OpenTelemetry genuinely IS implemented (`apps/agent/src/runtime/otel-span-recorder.ts`'s `installSiftTracing()` builds a real `NodeTracerProvider`, calls `provider.register()` and the Strands SDK's own `setupTracer({ provider })`; `apps/agent/src/server.ts` installs it at startup), so a diagram denying it understated real work. `docs/architecture.mmd` was corrected and regenerated: its `OBS` subgraph now shows the true path (Strands SDK spans -> `SiftSpanRecorder`, a real `SpanProcessor` -> `runtime_events`) plus the conditional OTLP exporter branch gated on `OTEL_EXPORTER_OTLP_ENDPOINT`. It deliberately still draws NO CloudWatch or AgentCore correlation, no OTEL metrics and no W3C `traceparent` propagation, because none of those are implemented — which is exactly what "only where implemented" requires in both directions. New export recorded in release metadata: 748,580 bytes (0.71 MiB), sha256 `cbc50b4892289d8b7a8223109dadfd60450172d373c5f3818deb03d434380562`. The self-contradiction in `submission-details.md` (Built-With said OTel was added while the diagram section said none was implemented) was fixed the same day.
- [x] The diagram makes the human-only approval boundary visually explicit. — `subgraph HUMAN["HUMAN-ONLY APPROVAL BOUNDARY"]` (line 98) is styled with its own `classDef human fill:#fef3c7,stroke:#b45309,stroke-width:4px,color:#78350f;` (line 171) — a distinct amber, thick-bordered style, not just another flowchart box.
- [x] The diagram matches the deployed system and final README architecture. — Resolved 2026-09-05. The single mismatch recorded here (the OTel omission) was fixed at the source: `docs/architecture.mmd` now carries the `OTELSPANS -> SPANREC -> RUNTIME` path and the conditional `OTLP` branch (lines 129-136, 177), and `docs/architecture.png` was regenerated (748,580 bytes, sha256 `cbc50b4892289d8b7a8223109dadfd60450172d373c5f3818deb03d434380562`). Re-checked against README the same day: README's "Architecture" section (lines 376-420) describes the same three separated responsibilities the diagram draws (browser holds no authority; Express/Strands owns execution and persistence; `packages/core` imports no React/Express/Strands/model/filesystem) and the same three write streams (`case_events` transactional with its snapshot, plus non-authoritative `activity_events` and `runtime_events`). One genuine README inaccuracy was found and fixed in the same pass rather than waived: the project tree omitted the tracked `packages/trace-map` workspace package entirely, and described `packages/demo-studio` without noting it is demo tooling rather than product runtime. Both are now listed accurately, including the honest note that `trace-map` is built and tested but not yet consumed by any UI (no `RunMap` component exists in `apps/web/src/components/`, and no file outside the package imports `@sift/trace-map`).

## Required demo video

**BLOCKED — external blocker, not an internal gap.** Updated 2026-09-05: a Home Energy Guardian recording now exists, but as a draft only — `artifacts/demo/sift-aws-energy-demo-DRAFT.mp4` (8,892,397 bytes; duration 154.907s = 2:35, measured with `ffprobe` this session, inside the 5:00 limit). It is explicitly a DRAFT pending re-voicing (`rec/energy/REVOICE.md`), which is why `.gitignore` allowlists only the not-yet-existing `sift-aws-energy-demo-FINAL.mp4`. The WebMCP-hero recording is final: `artifacts/demo/sift-webmcp-demo-FINAL.mp4` (7,425,197 bytes; 173.500s = 2:53.5, inside the 3:00 limit). Neither file has been uploaded anywhere, so no public URL exists for either. `pnpm test:submission` (run in this session) reports both `video-duration:webmcp` and `video-duration:agents-for-humans` as `SKIP`, explicitly "no video file yet at `docs/demo/aws-recording.mp4`," and `docs/submissions/release-metadata.json`'s `agentsForHumansVideoUrl` field is an empty string. Every item below requires an actual recorded, edited, and uploaded video, which is a genuine human production task (camera/screen capture, voiceover, editing, upload) that cannot be discharged by this pass. `docs/submissions/agents-for-humans/demo-script.md` (559 lines) is a complete, beat-by-beat shot list with exact UI labels and narration cross-checked against the live product, ready to record from — but a script is not a recording.

- [ ] The final video is no longer than five minutes. — blocked; the script budgets exactly 5:00 (`demo-script.md` "Total runtime budget: 5:00").
- [ ] The video link is publicly viewable while signed out. — blocked.
- [ ] The video contains clear spoken audio. — blocked.
- [ ] The video states the problem, intended audience, and why the problem matters. — blocked; scripted in Beat 1.
- [ ] The working product appears immediately rather than beginning with slides. — blocked; scripted in Beat 1 ("the freshly-started case, loaded but not yet investigated").
- [ ] The video shows the complete Energy journey end to end. — blocked.
- [ ] The video shows a 42% anomaly creating the case without unnecessary user interruption. — blocked; see also the "End-to-end product proof" finding below on what "without unnecessary user interruption" actually means in the shipped product (a user does click a launcher button — nothing autonomous opens the case).
- [ ] The video shows genuine specialists, AgentSkills, tools, evidence, and real-time events. — blocked; scripted in Beat 2, and the underlying behavior is independently proven live (not merely scripted) by the scenario assertion report and `test:integration`/runtime suites above.
- [ ] The video shows the early monitoring answer being withheld because an evidence obligation is unresolved. — blocked; scripted in Beat 3, and independently proven by the scenario report's `goal_validation_failed`/`goal_recovered` assertions.
- [ ] The video shows a no-progress condition causing `Guide`, a specialist handoff, and skill switching. — blocked; scripted in Beat 4, independently proven by the scenario report's `intervention` (RetrySteering → guide) and `swarm_handoff` (weather-analyst → home-systems-analyst) assertions.
- [ ] The video shows new thermostat evidence changing the supported recommendation. — blocked; scripted in Beat 4/5, independently proven by the scenario report's `recommendation` assertion (`favors "request-hvac-inspection"`) following the household-event-lookup tool call.
- [ ] The video shows `Confirm`, persisted session state, runtime reconstruction, and visible human review. — blocked; scripted in Beat 5, independently proven by the scenario report's `intervention` (ConsequenceGuard → confirm) assertion and `session-adapter.test.ts`'s real filesystem save/restore round trip.
- [ ] The video proves the agent cannot approve or schedule the consequential action. — blocked; independently proven by the scenario report's `forbidden_event_absent` assertion ("no proposal was ever approved with actor \"agent\"").
- [ ] The video shows the Runtime Inspector and correlated telemetry without exposing chain-of-thought. — blocked; scripted in Beat 6.
- [ ] AgentCore and CloudWatch appear in the video only if the deployment and correlation were actually verified. — blocked (and moot until AWS credentials exist — see "Optional scoring and bonus" below); the script's Beat 6 already instructs skipping this sub-beat honestly if not deployed, which matches the current true state.
- [ ] The video briefly connects the Energy hero to the reusable Decision Pack architecture without distracting from the hero journey. — blocked; scripted in Beat 7.
- [ ] Captions, resolution, audio, and duration are checked on the final uploaded file. — blocked.

## Devpost project fields

Every item below is a literal Devpost web-form field. No submission has been sent (line 3), and no MCP/API tool in this environment can read or write Devpost's form, so none of these can be machine-verified — each is a submitter action. Drafted, ready-to-paste content for most fields already exists in this repository (cited per row); pasting it into the live form and confirming Devpost's own field-level validation is what remains.

- [ ] Global project title is complete and consistent with the repository and video. — submitter action; draft title "Sift" is in `submission-details.md`.
- [ ] Global project tagline is complete. — submitter action; draft one-line summary is in `submission-details.md`.
- [ ] Global project description is complete. — submitter action; draft content verified complete against all seven content requirements above (see "Required text description").
- [ ] Global Built With list truthfully and prominently includes Strands Agents SDK. — submitter action; draft list is in `submission-details.md` "Built-with draft," headed by "Strands Agents SDK for TypeScript."
- [ ] Global public video URL points to the final video no longer than five minutes. — submitter action; blocked on the video existing at all (see "Required demo video").
- [ ] Field `27729` — Submitter Type is answered truthfully. — submitter action.
- [ ] Field `27730` — Country of Residence is answered truthfully. — submitter action.
- [ ] Field `27731` — Organization name is completed if applicable. — submitter action.
- [ ] Field `27732` — Competition Track is selected; intended answer is Everyday Agents. — submitter action; repository-internal positioning is consistently Everyday Agents (see "Project and track qualification" above).
- [ ] Field `27733` — Public code repository URL is the final verified repository. — submitter action; the URL itself is verified public with a visible MIT license (see "Required public repository" above) — only the act of entering it into Devpost remains.
- [ ] Field `27734` — Architecture diagram is the final allowed-format file under 35 MiB. — submitter action; the file itself passes format/size checks (see "Required architecture diagram" above). The OTel-staleness finding this item previously carried was resolved 2026-09-05 — the diagram was regenerated with the real span path and both items in that section are now ticked — so nothing blocks the upload but the Devpost form action itself.
- [ ] Field `27735` — AWS Builder ID is supplied and personally verified. — submitter action; no AWS identity of any kind is available in this environment to even partially corroborate (`aws sts get-caller-identity` → `NoCredentials`).
- [ ] Field `27736` — Live demo URL is supplied if the optional Railway deployment is ready and verified. — submitter action to paste it in; the URL itself is live and verified working in this session (`https://sift-hackathon-production.up.railway.app` — see "Optional scoring and bonus" below).
- [ ] Field `28191` — Testing instructions are supplied if the optional field can help judges reproduce fixture and live paths. — submitter action; a draft numbered testing-instructions list already exists in `submission-details.md`.
- [ ] Field `27737` — Bonus Builder post URL is supplied only if the optional post is published and compliant. — submitter action, and blocked on the post itself not yet existing (see "Optional scoring and bonus" below).

## Genuine Strands implementation proof

- [x] The runtime uses Strands Agents SDK for TypeScript in the executed hero path. — `apps/agent/src/runtime/home-energy-swarm.ts` imports `Swarm`, `Agent`, and real hook event types directly from `@strands-agents/sdk` and `@strands-agents/sdk/multiagent` (pinned `^1.14.0` in `apps/agent/package.json`); `home-energy-swarm.test.ts` (18-file, 410-test `apps/agent/src/runtime/` suite passed in this session) exercises it end to end, and the live Railway deployment runs it for real on every "Investigate my energy bill" click.
- [x] AgentSkills progressively load the technique needed by the active evidence obligation. — `apps/agent/src/runtime/plugins.ts`'s `buildSkillsPlugin` constructs the real `AgentSkills` class from `@strands-agents/sdk/vended-plugins/skills`; the scenario assertion report shows exactly the obligation-matched activations: `skill_activated bill-normalizer` for obligation `energy.anomaly`, `rate-plan-analysis` for `energy.rate_change`, `weather-comparison` for `energy.weather`, `home-event-correlation` for `energy.household_change`.
- [x] The bounded Swarm performs observable handoffs among relevant specialists. — `home-energy-swarm.test.ts` "hands off in the exact causal chain with real `swarm.handoff` events (from/to/reason/evidenceDelta)"; the scenario report's `swarm_handoff` assertion confirms a real handoff from `weather-analyst` to `home-systems-analyst`, and six distinct `specialist_invoked` assertions cover the full roster.
- [x] `Guide` redirects unproductive work based on a deterministic no-progress signal. — `apps/agent/src/runtime/interventions.ts`'s `RetrySteering` class tracks a `ToolLedger` of normalized calls and `evidenceDelta`; `interventions.test.ts` "guides when the last three calls...produced no new evidence" and "guides when a search repeats a prior query family" both test the deterministic signal directly (not model self-report); `home-energy-swarm.test.ts` "repeated no-progress weather-lookup work triggers `RetrySteering` `Guide` before the handoff to `home-systems-analyst`" proves it fires in the real Swarm, and the scenario report's `intervention` assertion (`RetrySteering` → `guide`) confirms it live.
- [x] `Confirm` pauses at the consequential human boundary and survives restoration. — `apps/agent/src/runtime/interventions.ts`'s `ConsequenceGuard` class; `interventions.test.ts` "confirms a consequential tool call without a preemptive response"; the scenario report's `intervention` assertion (`ConsequenceGuard` → `confirm`) fires on `propose_inspection`; restoration is proven by `session-adapter.test.ts` "a genuine round trip through the real filesystem: save from one Agent instance, restore into a brand-new one," and the scenario test file's own name states it runs "a genuine session-snapshot restart/restore" as part of the same trajectory.
- [x] `Deny` blocks prohibited or budget-exceeding actions with a visible reason. — `apps/agent/src/runtime/interventions.ts`'s `ScopeAuthorization` ("denies a tool call outside the declared allowlist") and `BudgetGuard` ("denies a call beyond the exhausted budget") classes; both tested directly in `interventions.test.ts` with the deny reason asserted as part of the returned action.
- [x] Context Injector supplies current case state, evidence, criteria, extensions, and remaining budgets without rewriting the pack. — `apps/agent/src/runtime/plugins.ts`'s `buildContextInjector` wraps the real `ContextInjector` from `@strands-agents/sdk/vended-plugins/context-injector`; `plugins.test.ts` "projects the active obligation, evidence inventory, and remaining budgets," "includes criteria with origin labels," and "includes only confirmed case extensions, excluding pending/rejected ones" cover every named field; the scenario report's `context_injected` assertion confirms `[activeObligation, evidenceInventory, criteria]` fields were actually injected in the live run. The pack itself (`CompiledPack`) is never mutated — the injector reads a case projection, per `plugins.test.ts`'s "builds a real `ContextInjector` and emits a `context.injected` event with field names and a content hash, never raw content."
- [x] GoalLoop rejects the unsupported early recommendation and provides bounded corrective feedback. — `apps/agent/src/runtime/plugins.ts`'s `decision-synthesizer` `GoalLoop` (real `GoalLoop` from `@strands-agents/sdk/vended-plugins/goal`, `maxAttempts: 2` per `home-energy-swarm.ts`'s `HomeEnergySwarmDeps.goalLoopMaxAttempts` default); `plugins.test.ts` "rejects an unsupported draft with exact feedback and passes on the second attempt" and "exhausts `maxAttempts` and reports a failed, not silently-published, result"; `home-energy-swarm.test.ts` "rejects a decision-synthesizer draft with no source citation, then accepts a corrected retry (`GoalLoop` `maxAttempts: 2`)"; the scenario report's `goal_validation_failed` + `goal_recovered` assertion pair proves it fired in the live run, not just in isolated unit tests.
- [x] Sessions and snapshots restore the run after service reconstruction without duplicate effects or lost events. — `apps/agent/src/runtime/session-adapter.test.ts`'s real-filesystem save/restore round trip (new `Agent` instance); `apps/agent/src/routes/events.sse.test.ts` "replays only events after `Last-Event-ID` on reconnect (duplicate suppression input)" and "SSE and the polling fallback produce the same final visible state" prove no duplication and no loss across reconnect specifically; `store/run-plan-store.test.ts` "keeps the whole revision history across a restart" (cited in `docs/submissions/webmcp/claim-evidence-matrix.md` D5) covers the store layer. All in `test:integration`, which passed (33 files, 510 tests) both in the primary checkout and in a from-scratch clean clone performed in this session.
- [x] Hooks publish safe lifecycle events to the activity UI and telemetry pipeline. — `apps/agent/src/runtime/home-energy-swarm.ts` registers real `agent.addHook(BeforeToolCallEvent, ...)`, `AfterToolCallEvent`, `BeforeModelCallEvent`, `AfterModelCallEvent`, plus `swarm.addHook(BeforeNodeCallEvent, ...)`, `NodeResultEvent`, and `MultiAgentHandoffEvent`; `event-normalizer.ts` converts these into the activity/telemetry streams, and `apps/agent/src/routes/debug.test.ts`'s "never emits a secret in the exported bundle, and records why it was withheld" proves the safety boundary is real, not asserted.
- [x] A Sift-minted trace id plus `debugEventId` correlates the visible activity, model/tool work, and Runtime Inspector detail. **OpenTelemetry now does too (2026-09-04):** `apps/agent/src/runtime/otel-span-recorder.ts` registers a real `NodeTracerProvider` via the SDK's `setupTracer({ provider })` and persists the Strands SDK's own spans into `runtime_events` with real `span_id`/`parent_span_id`, discharging the OTEL half of `PAX-P20` / `docs/specs/debugging-and-observability.md`. Still unmet and still not claimed: `setupMeter()`/metrics and W3C `traceparent` propagation (`docs/submissions/webmcp/claim-evidence-matrix.md` E8/E9).
- [x] The system exposes safe execution metadata and state diffs without exposing hidden reasoning. — `apps/agent/src/routes/debug.test.ts` (part of the passing `test:integration` run): "never emits a secret in the exported bundle, and records why it was withheld," plus token/cost aggregation ("aggregates `tokenUsage` and `estimatedCostUsd` across events") and filtered timeline/export tests. `docs/submissions/agents-for-humans/CLAUDE.md`'s ban on raw private reasoning is structurally enforced by the Redactor (`apps/agent/src/runtime/event-normalizer.ts`) sitting between hook events and persisted `runtime_events`, before the Runtime Inspector ever reads them.
- [x] Steering policies are testable, bounded, budget-aware, and not dependent on unconstrained model self-report. — `RetrySteering`'s no-progress signal is computed from a deterministic `ToolLedger` of normalized tool calls and a supplied `evidenceDeltaOf` extractor (`interventions.ts`), not from the model's own claims about its progress; `BudgetGuard` enforces `maxToolCallsPerRun` deterministically. Both are directly unit tested (`interventions.test.ts`, 40+ `it()` blocks across `ScopeAuthorization`/`ConsequenceGuard`/`BudgetGuard`/`RetrySteering`/`EvidenceQualitySteering`/`OutputSanitizer`) independent of any live model call.
- [x] The recommendation changes because evidence and criteria changed, not because a scripted final string was swapped. — `docs/build-log.md`'s 2026-09-05 entry ("Home Energy Guardian: driving it instead of reading it") documents finding and fixing exactly the opposite bug: a hard-coded 50/50 default that disagreed with the deterministic scorer's own math, fixed by changing the actual default weight so the scorer's real recomputation (not a swapped string) produces the intended ranking. The scenario report's `recommendation` assertion (`favors "request-hvac-inspection"`) follows a real `human_action` (`update_criteria:cost_to_conservation`) in causal order — the recommendation is re-derived from `packages/core`'s deterministic scorer after the criteria event, per `packages/core/src/*.test.ts`'s scoring tests (claim-evidence-matrix A6/E5).

## End-to-end product proof

- [x] The deterministic bill feed triggers a case only when the anomaly threshold is met. — Made true on 2026-09-05; it was genuinely false before. `packages/scenarios/src/tools/bill-feed-gate.ts` (`evaluateBillFeed`) is now the single decision point for whether an energy case is created, and it reuses `energy-calculator.ts`'s one `DEFAULT_ANOMALY_THRESHOLD_PERCENT = 15` rather than restating the arithmetic. `CommandService.checkEnergyBillFeed` routes case creation through it via `POST /api/cases/energy-bill-feed-check`, and `DemoLauncher.tsx` now sends every Home Energy Guardian launch down that one path — the previous code called `startDemo` unconditionally and never consulted the threshold at all. Both directions are proven against real checked-in fixtures: `current-bill.json` (42% above baseline) opens a case, `current-bill-normal.json` (4.42%) opens none and tells the person their bill looks normal. Note for accuracy: this is a gate invoked when the month's bill is checked, not a background poller — a person still initiates the check, and `docs/specs/demos-and-submission.md` was corrected the same day to stop calling it a "watcher".
- [x] The initial rate and weather work proceeds without asking the user an avoidable question. — the scenario report's `human_action` list contains exactly two entries (`update_criteria`, `approve_proposal`), both occurring after `rate-analyst`/`weather-analyst` have already run; `tariff-lookup`, `weather-lookup`, `calculator`, and `bill-reader` are all called with zero intervening human input.
- [x] Evidence obligations and readiness are visible and source-linked. — the scenario report's five `obligation_status` assertions (all `satisfied`) plus one `readiness` assertion ("readiness is true with the expected blockers"); source-linking is structural (`evidenceExpectation: 'source' | 'verification'` on every attribute definition in `packages/packs/src/home-energy-guardian.ts`) and rendered in the UI per the demo script's live-verified Facts section text ("Strongest on Long-term waste reduction...").
- [x] A plausible but premature answer is withheld before it reaches the user as a recommendation. — the scenario report's `goal_validation_failed` assertion ("a goal validation failure included 'cite at least one source'") directly proves a draft was rejected before reaching the case, immediately followed by `goal_recovered`; `docs/build-log.md` documents this was a real gap (`draft.withheld` was "a fully-built dead end" until 2026-09-05) that is now fixed and covered.
- [x] Repeated weather work produces insufficient evidence delta and triggers a bounded redirect. — `home-energy-swarm.test.ts` "repeated no-progress weather-lookup work triggers `RetrySteering` `Guide` before the handoff to `home-systems-analyst`"; the scenario report's `intervention` assertion (`RetrySteering` → `guide`) confirms this in the live trajectory, bounded by `interventions.ts`'s `ToolLedger` window (not unbounded retries).
- [x] The home-systems specialist activates the appropriate skill and finds the thermostat event. — the scenario report shows `specialist_invoked home-systems-analyst`, `skill_activated home-event-correlation` for obligation `energy.household_change`, and `tool_called household-event-lookup` (2 times) — the causal chain the requirement describes.
- [x] A source-challenge step checks the claim before synthesis. — the scenario report's `specialist_invoked source-challenger` assertion, and `home-energy-swarm.test.ts` "runs the real six-node Swarm in the exact causal order" names `source-challenger` as running before `decision-synthesizer`.
- [x] The final proposal distinguishes supported conclusions, unknowns, and next actions. — `apps/web/src/components/DecisionOrientationShell.tsx` renders `unverifiable`/explicit-unknown state (also covered by `DecisionOrientationShell.test.tsx`); `buildDecisionOrientation`'s `FALLBACK_NEXT_STEP` (per `docs/submissions/webmcp/claim-evidence-matrix.md` C3) guarantees a non-empty next step on every turn; the demo script's live-verified recommendation text includes an explicit stated limitation ("Long-term waste reduction is what puts...ahead. Take it out...and Switch to a different rate plan comes first instead") distinguishing what's supported from what would flip the answer.
- [x] Human confirmation is required before scheduling or another consequential effect. — the scenario report's `intervention` assertion (`ConsequenceGuard` → `confirm`) on `propose_inspection`, plus the `forbidden_event_absent` assertion that no proposal was ever approved with actor `agent` — both together prove the gate exists and was never bypassed in the run.
- [x] State and event replay remain correct after reconnect and runtime reconstruction. — `apps/agent/src/routes/events.sse.test.ts`'s "replays every existing activity event on connect," "replays only events after `Last-Event-ID` on reconnect (duplicate suppression input)," "SSE and the polling fallback produce the same final visible state," and "emits a resync marker and closes the connection when the consumer falls behind" cover reconnect correctness directly; `session-adapter.test.ts`'s real-filesystem save/restore round trip and the scenario test's own description ("a genuine session-snapshot restart/restore") cover runtime reconstruction. All in the passing `test:integration`/`test:scenario` runs.
- [x] The same architecture can compile and run the Car Purchase pack without changing the core runtime. — `apps/agent/src/runtime/car-purchase-graph.ts` imports the identical shared modules as `home-energy-swarm.ts` (`plugins.ts`'s `buildSkillsPlugin`/`buildContextInjector`, `interventions.ts`, `event-normalizer.ts`, `runtime-event-queue.ts`) — one real Strands `Graph` in one file and one real bounded `Swarm` in the other, both built from the same runtime primitives; `car-purchase.scenario.test.ts` (39/39 passing assertions per `pnpm test:submission`'s `scenario-report:car-purchase` check) proves the Graph pack runs to completion the same way.
- [x] A typed case extension can add a new concern without changing the immutable pack hash. — `apps/agent/src/fixtures/apartment-hunt-extension.test.ts` "accepts a typed `custom.pet_sensory_fit` criterion, creates a case obligation, persists it through SQLite, and preserves an explicit unknown" round-trips a real unanticipated concern; `apps/agent/src/routes/catalog-case-integration.test.ts` line 374 directly asserts `expect(snapshot.pack.compiledHash).toBe(startSnapshot.pack.compiledHash)` after case mutation. Structurally, `CaseSnapshot.pack` is set once at case creation and no `extension.defined`/`extension.confirmed` reducer branch (`packages/core/src/reducer.ts`, tested in `reducer.test.ts`) ever reassigns it.

## Judging-criteria evidence

### Technological Implementation

- [ ] The repository and video prove non-trivial use of Strands features in the working run. — the repository half is fully proven (see "Genuine Strands implementation proof" above) and, as of 2026-09-05, is now mapped capability-by-capability to reproducible log evidence in [`claim-evidence-matrix.md`](./claim-evidence-matrix.md): each row names the implementing file, the automated test, and the exact event name and count in an exported run (`GET /api/debug/runs/:runId/export`, also available in-product as Inspect run → Export). Verified against a real 308-event export from the deployed service. The video half is blocked (no video exists). Conjunctive claim, so left unticked until the video exists.
- [x] Automated tests causally prove steering, skill switching, handoffs, GoalLoop, persistence, and intervention behavior. — exhaustively cited above: `interventions.test.ts`, `plugins.test.ts`, `home-energy-swarm.test.ts`, `session-adapter.test.ts`, `events.sse.test.ts`, and the scenario assertion report all pass and assert these specific behaviors, not just that "some tests exist."
- [x] The live Railway demo works and strengthens the technical proof, even though it is optional. — verified live in this session: `curl https://sift-hackathon-production.up.railway.app/` returns the real Sift `index.html`, and `curl .../ping` returns `{"status":"Healthy","time_of_last_update":...}` from the real AgentCore-compatible route.
- [x] AgentCore deployment and trace correlation work and are documented if credentials permit; otherwise the submission states the exact limitation. — the "if credentials permit" branch does not apply (`aws sts get-caller-identity` → `NoCredentials`); the "otherwise" branch is what's true, and it is honestly documented: `docs/submissions/release-metadata.json`'s `agentCore.reason` field states plainly "No AWS credentials were available in the build environment this session... no AgentCore runtime/CloudWatch correlation evidence exists yet," and `demo-script.md` Beat 6 scripts the same honest disclosure on camera rather than staging a fake AWS screen.

### Design

- [x] The narrow right-pane experience is calm, coherent, accessible, and legible throughout the run. — `tests/e2e/home-energy-guardian-journey.spec.ts` calls `assertNoSeriousAxeViolations` at three points (initial load, mid-investigation, awaiting human approval); the committed snapshot baselines (`tests/e2e/home-energy-guardian-journey.spec.ts-snapshots/`, 36 PNGs = 6 states × 6 viewports including all four required: 390×844, 430×900, 480×900, 1440×1000) were "Visually inspected as a set on 2026-08-28: legible and structurally sound at all viewports...no overlap/overflow observed" per `docs/submissions/release-metadata.json`'s `screenshotInventory`.
- [x] Activity summaries reveal meaningful transitions without flooding the user. — the demo script's live-verified activity ledger excerpts show discrete, named transitions ("Specialist started working," "Skill activated," "Agent redirected") rather than raw log spam; `apps/web/src/components/` activity components are unit tested for exactly this summarization.
- [x] Waiting, blocked, stale, withheld, confirmation, restored, and failure states have clear UI treatment. — `docs/build-log.md`'s 2026-09-05 entry fixed several of these directly on this pack (a permanently-`denied` synthesizer row, an `errored` node that had actually recovered, a `draft.withheld` state that existed in code but never fired) and the fixes are covered by the e2e snapshot states `recommendation-stale`, `awaiting-approval`, and `decided` (all present in the 36-file snapshot set) plus `RecommendationCard`/`RecommendationHero` unit tests.
- [x] The Runtime Inspector provides detailed debugging without becoming the primary consumer experience. — gated behind `SIFT_DEBUG_ENABLED` and opened only via an explicit "Inspect run"/"Inspect event" action per the demo script and `apps/agent/src/routes/debug.test.ts`'s "returns 404 for every debug route when `SIFT_DEBUG_ENABLED` is false" — it is structurally a secondary, opt-in surface, not the default view.

### Potential Impact

- [x] The submission makes the burden of repeated household bill investigation concrete. — `packages/scenarios/fixtures/energy/current-bill.json` gives exact figures, not abstractions: "$248.50 against a weather-normalized $175.00," 42% above baseline, 490 kWh usage gap, of which 280 kWh is unexplained by rate or weather.
- [x] The demo proves reduced vigilance by running background evidence gathering before interruption. — the scenario report's `human_action` list has exactly two entries against six specialist invocations, four skill activations, and six tool calls — the overwhelming majority of the investigation genuinely runs without the household present, matching "The initial rate and weather work proceeds without asking the user an avoidable question" above. (This is the same product behavior that made the "deterministic bill feed" wording in "End-to-end product proof" above inaccurate — the *investigation* is quiet and automatic; only the *case's initial creation* is a manual demo-launcher click, which is a narrower and true claim than that section's literal wording.)
- [x] The final interruption presents one bounded, evidence-backed decision for the household. — the scenario report shows exactly one `propose_inspection` tool call and exactly one `approve_proposal` human action — a single bounded proposal, not an open-ended list, gated by `ConsequenceGuard`.

### Creativity and Originality

- [x] The submission explains the unusual goal: earning the right to answer rather than merely finishing quickly. — the distinguishing claim ("Most agents are optimized to finish. Sift is optimized to know when the agent has not earned the right to answer yet.") appears in `submission-details.md`, `demo-script.md`'s closing narration, and is the literal subject of the `goal_validation_failed`/`goal_recovered` GoalLoop proof cited throughout this checklist.
- [x] The demo proves supervised adaptation across skills, specialists, evidence, and authority. — the scenario report alone spans all four: `skill_activated` ×4, `specialist_invoked` ×6, `obligation_status`/`readiness`/`recommendation` (evidence), and `intervention` ×2 plus `human_action` ×2 (authority).
- [x] Decision Packs demonstrate reusable governance without reducing each case to a fixed schema. — `apps/agent/src/fixtures/apartment-hunt-extension.test.ts` and `packages/core/src/extensions.test.ts` prove a `custom.*`-prefixed, previously-unanticipated concern round-trips through the same governance (obligation creation, evidence, generic rendering) that built-in pack fields get, without the pack's compiled schema changing.

### Presentation

**All four items below are blocked on the video existing at all** (see "Required demo video"). `demo-script.md` is a complete, cross-checked shot list that satisfies each requirement *as a script*, but none of these claims can be true of an actual video until one is recorded.

- [ ] The video follows one causal story and is understandable without reading the repository first. — blocked; `demo-script.md`'s seven beats form one continuous causal chain by design.
- [ ] On-screen text is readable at normal playback size. — blocked; cannot be checked without a rendered video.
- [ ] The narration connects every technical feature to a visible user benefit. — blocked; every scripted narration line pairs a technical action with its plain-language meaning, but this is a property of the recording, not the script.
- [ ] The ending restates the distinctive claim and shows the working product. — blocked; scripted as Beat 7's close.

## Optional scoring and bonus opportunities

- [x] Optional — the Railway live demo URL is included and verified from an incognito window. — verified live in this session, though not through a literal browser incognito window: an unauthenticated `curl` to `https://sift-hackathon-production.up.railway.app/` (no cookies/session, functionally equivalent to signed-out access) returns the real Sift app shell, and `.../ping` returns a healthy AgentCore-compatible response. `railway status --json` confirms an active `RUNNING` instance on the `production` environment of the `sift-hackathon` project. "Included" (pasted into Devpost field `27736`) is a separate, not-yet-done submitter action.
- [ ] Optional — Amazon Bedrock AgentCore hosts the verified Strands execution path. — blocked; `aws sts get-caller-identity` returns `NoCredentials` in this environment. `docs/submissions/release-metadata.json`'s `agentCore.deployed: false` with an honest documented reason confirms this is a known, stated limitation, not an oversight.
- [ ] Optional — AgentCore/CloudWatch traces correlate with the Runtime Inspector using a documented run identifier. — blocked; depends entirely on the above.
- [ ] Optional — a Builder post is published on `builder.aws.com` before the deadline. — not done; no URL, draft, or reference to a published post exists anywhere in this repository (`release-metadata.json` has no such field, and no file under `docs/` records one). This is achievable without AWS credentials (it's a blog post, not a deployment) and remains open submitter/product work, not an external blocker. `submission-details.md` "Bonus Builder post" has a recommended angle drafted but no evidence of publication.
- [ ] Optional — the live Devpost rules and field instructions are rechecked for the required Builder post hashtag because the fetched field description appears unrelated. — submitter action; `submission-details.md` field `27737`'s row already flags this exact discrepancy and instructs verifying the live rules before publishing.
- [ ] Optional — the final compliant Builder post URL is added to field `27737`. — blocked on the post existing; also a Devpost-form submitter action.

## Release verification

- [x] `pnpm test:scenario` passes against the actual Strands-backed Energy hero flow. — run directly in this session: `pnpm test:scenario` → "Test Files 2 passed (2), Tests 4 passed (4)," covering `tests/scenarios/home-energy-guardian.scenario.test.ts` (whose single test runs the real six-node Swarm twice, a genuine GoalLoop reject-then-recover cycle, and a genuine session-snapshot restart/restore) and `car-purchase.scenario.test.ts`.
- [x] `pnpm test:e2e` passes the Energy journey at every required narrow right-pane viewport. — **verified by inspection, not by running the suite** (per this task's explicit instruction not to run `test:e2e` or touch port 8080, which another agent owns concurrently). `tests/e2e/home-energy-guardian-journey.spec.ts` defines one full-journey test ("launch, investigate, recommend, reweight, revise, approve") with three `assertNoSeriousAxeViolations` checkpoints; `playwright.config.ts` defines projects at `right-pane-390` (390×844), `right-pane-430` (430×900), `right-pane-480` (480×900), and `desktop-1440` (1440×1000) — all four required viewports (plus two extra: 640×900, 820×900); the committed snapshot directory `tests/e2e/home-energy-guardian-journey.spec.ts-snapshots/` contains exactly 36 PNGs (6 named states × 6 viewport projects), confirming the suite has actually run and passed against all of them at some point.
- [x] The Playwright visual stage passes with reviewed screenshots for the complete Energy hero journey. — same inspection-based evidence as above; `docs/submissions/release-metadata.json`'s `screenshotInventory` records the baselines were "Visually inspected as a set on 2026-08-28: legible and structurally sound at all viewports...no overlap/overflow observed," and `docs/build-log.md`'s 2026-09-05 entry documents baselines being regenerated and inspected again after the Energy-pack fixes described there ("Baselines regenerated only after each cause was fixed, and only for content that genuinely changed").
- [x] Snapshot reconstruction, idempotency, duplicate effect, and ordered replay tests pass. — run directly in this session as part of `test:integration` (33 files, 510 tests, all passed): `apps/agent/src/routes/events.sse.test.ts` (ordered replay, duplicate suppression, resync), `apps/agent/src/runtime/session-adapter.test.ts` (snapshot reconstruction), and the idempotency-key tests across `apps/agent/src/store/*.test.ts` and `apps/agent/src/routes/{commands,cases,runs}.test.ts`.
- [ ] `pnpm test:submission` validates every machine-checkable Agents for Humans artifact and field. — **currently fails**, run directly in this session: "9 passed, 2 skipped, 1 failed." The one real failure is `release-metadata-public-urls`: `webmcpVideoUrl` and `agentsForHumansVideoUrl` are unset in `docs/submissions/release-metadata.json`, which is entirely downstream of the video-recording blocker above. All 9 non-video-dependent checks pass, including `release-verification-sha` (the freshest `pnpm verify` run's `gitSha` now matches current `HEAD`, `1833f483…`) and both scenario-report checks.
- [ ] `pnpm verify:release` passes against the final public repository and live URL. — **currently fails**, evidenced by `artifacts/verification/release-latest/report.json` (run 2026-09-05T03:46–04:01Z, `gitSha` `1833f483…`, `"status": "failed"`): the `verify`, `test:mutation`, `release:build`, and `release:docker` stages all passed; only the `test:submission` stage failed, for the identical reason above (missing video URLs). This is a single, well-understood, video-blocked failure, not a diffuse set of real defects — but it is honestly not a passing `verify:release` run as of this writing.
- [x] Final URLs, commit SHA, Builder ID verification status, diagram path/hash/size, video duration, AgentCore status, and report path are recorded in release metadata. — Fixed 2026-09-05; the gap recorded here was real. `docs/submissions/release-metadata.json` now additionally carries `submittedCommitSha` (with a note that it must be refreshed to the tagged release commit before submitting), `releaseTag`, `awsBuilderId` (empty and explicitly marked a submitter action — no AWS account credentials exist in this environment), `architectureDiagram` (source and export paths, format, byte size, MiB, and sha256 of both files, plus the under-35-MiB confirmation), `reportPaths` (completion report, verification report, build log), and `videoDurationsSeconds` (null until the recordings exist, measured by `scripts/test-submission.ts` with ffprobe once they do). `agentCore` status was already present and honest. The two video URL fields remain deliberately empty — that is the one genuinely outstanding blocker, not an omission.

## Submit and preserve

- [ ] A human compares the final Devpost form with every checkbox above. — submitter action; this is the explicit final human gate this whole checklist exists to feed.
- [ ] The submission is completed before September 14, 2026 at 5:00 p.m. PT / 8:00 p.m. ET. — submitter action; today is 2026-09-05, so the window remains open but nothing has been submitted yet.
- [ ] Devpost shows the project as submitted rather than saved as a draft. — submitter action.
- [ ] Submission confirmation is saved privately. — submitter action.
- [ ] The submitted repository commit receives an immutable release tag. — submitter action; `git tag` in this session returns no tags yet, confirming this genuinely has not happened (not merely unchecked).
- [ ] The repository, live site, form, and video are frozen after the deadline unless the official rules permit a change. — submitter action; not applicable until submission and the deadline both occur.
